import { ComponentFixture, TestBed } from '@angular/core/testing';

import { VisaRecommendationFormComponent } from './visa-recommendation-form.component';

describe('VisaRecommendationFormComponent', () => {
  let component: VisaRecommendationFormComponent;
  let fixture: ComponentFixture<VisaRecommendationFormComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ VisaRecommendationFormComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(VisaRecommendationFormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
