import { ComponentFixture, TestBed } from '@angular/core/testing';

import { VissaApprovalComponent } from './vissa-approval.component';

describe('VissaApprovalComponent', () => {
  let component: VissaApprovalComponent;
  let fixture: ComponentFixture<VissaApprovalComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ VissaApprovalComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(VissaApprovalComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
